@extends('layouts.app')
@section('title', 'Facilities & Resources')
@section('breadcrumb')<li class="breadcrumb-item">Administration</li><li class="breadcrumb-item active" aria-current="page">Facilities</li>@endsection
@section('content')
<div class="container-fluid px-0"><div class="d-flex flex-wrap justify-content-between align-items-start gap-3 mb-4"><div><h1 class="h3 fw-bold mb-1">Facilities &amp; Resources</h1><p class="text-muted mb-0">Manage the campus facilities shown in the university resource directory.</p></div><a href="{{ route('admin.facilities.create') }}" class="btn btn-crimson"><i class="bi bi-plus-circle me-1"></i>Add facility</a></div>
@if(session('success'))<div class="alert alert-success" role="status">{{ session('success') }}</div>@endif
<div class="card custom-card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th scope="col">Facility</th><th scope="col">Slug</th><th scope="col">Status</th><th scope="col" class="text-end">Actions</th></tr></thead><tbody>@forelse($facilities as $facility)<tr><td><strong>{{ $facility->title }}</strong><div class="small text-muted">{{ Str::limit($facility->description, 100) }}</div></td><td>{{ $facility->slug }}</td><td><span class="badge {{ $facility->is_active ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $facility->is_active ? 'Active' : 'Inactive' }}</span></td><td class="text-end"><a href="{{ route('admin.facilities.edit', $facility) }}" class="btn btn-sm btn-outline-primary" aria-label="Edit {{ $facility->title }}"><i class="bi bi-pencil" aria-hidden="true"></i></a></td></tr>@empty<tr><td colspan="4" class="text-center py-4 text-muted">No facilities have been configured.</td></tr>@endforelse</tbody></table></div>@if($facilities->hasPages())<div class="card-footer bg-white">{{ $facilities->links() }}</div>@endif</div></div>
@endsection
