@extends('layouts.app')
@section('title', 'System Health')
@section('breadcrumb')<li class="breadcrumb-item">Super Admin</li><li class="breadcrumb-item active" aria-current="page">System Health</li>@endsection
@section('content')
<div class="container-fluid px-0"><div class="mb-4"><h1 class="h3 fw-bold mb-1">System Health &amp; Operations</h1><p class="text-muted mb-0">Non-secret environment, database, queue, storage, and integration checks.</p></div><div class="row g-3 mb-4">@foreach([['Database', $database], ['Environment', $environment], ['Queue', $queue], ['Failed jobs', $failedJobs]] as [$label, $value])<div class="col-6 col-xl-3"><div class="card custom-card h-100"><div class="card-body"><div class="small text-muted">{{ $label }}</div><strong class="fs-5 text-break">{{ $value }}</strong></div></div></div>@endforeach</div><div class="row g-4"><div class="col-lg-6"><div class="card custom-card h-100"><div class="card-header bg-white"><h2 class="h5 fw-bold mb-0">Configuration safety</h2></div><div class="card-body"><dl class="row small mb-0"><dt class="col-7">Debug mode</dt><dd class="col-5">{{ $debug ? 'Enabled' : 'Disabled' }}</dd><dt class="col-7">Free storage</dt><dd class="col-5">{{ number_format($storageFree / 1073741824, 2) }} GB</dd></dl><p class="small text-muted mt-3 mb-0">Environment variables and secret values are intentionally never displayed.</p></div></div></div><div class="col-lg-6"><div class="card custom-card h-100"><div class="card-header bg-white"><h2 class="h5 fw-bold mb-0">Integration toggles</h2></div><div class="card-body"><ul class="list-group list-group-flush">@foreach($integrations as $label => $configured)<li class="list-group-item px-0 d-flex justify-content-between gap-3">{{ $label }}<span class="badge {{ $configured ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $configured ? 'Configured' : 'Not configured' }}</span></li>@endforeach</ul></div></div></div></div></div>
@endsection