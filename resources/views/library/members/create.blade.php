@extends('layouts.app')

@section('title', 'Add Library Member')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Library workspace</p><h1 class="h2 mb-1">Add Library Member</h1><p class="text-muted mb-0">Grant library access to an existing university user account.</p></div><section class="card border-0 shadow-sm"><form method="POST" action="{{ route('library.members.store') }}"><div class="card-body row g-3">@csrf<div class="col-12"><label for="member-user" class="form-label">User account</label><select id="member-user" name="user_id" class="form-select" required><option value="">Select user</option>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }} ({{ $user->email }})</option>@endforeach</select></div><div class="col-12 col-md-4"><label for="membership-type" class="form-label">Membership type</label><select id="membership-type" name="membership_type" class="form-select" required>@foreach(['student' => 'Student', 'staff' => 'Staff', 'teacher' => 'Faculty', 'external' => 'External'] as $value => $label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></div><div class="col-12 col-md-4"><label for="membership-start" class="form-label">Start date</label><input id="membership-start" type="date" name="membership_start_date" value="{{ old('membership_start_date', now()->toDateString()) }}" class="form-control" required></div><div class="col-12 col-md-4"><label for="membership-end" class="form-label">End date</label><input id="membership-end" type="date" name="membership_end_date" value="{{ old('membership_end_date') }}" class="form-control"></div></div><div class="card-footer bg-white"><button class="btn btn-primary" type="submit">Create membership</button></div></form></section></div>
@endsection
