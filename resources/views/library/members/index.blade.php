@extends('layouts.app')

@section('title', 'Library Members')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4"><div><p class="text-uppercase text-muted small fw-semibold mb-1">Library workspace</p><h1 class="h2 mb-1">Library Members</h1><p class="text-muted mb-0">Manage active student, staff, and faculty library access.</p></div><a href="{{ route('library.members.create') }}" class="btn btn-primary"><i class="bi bi-person-plus me-1" aria-hidden="true"></i>Add member</a></div><form class="card border-0 shadow-sm mb-4" method="GET"><div class="card-body d-flex gap-2"><label for="member-search" class="visually-hidden">Search members</label><input id="member-search" name="search" value="{{ request('search') }}" class="form-control" placeholder="Search member name"><button class="btn btn-outline-primary" type="submit">Search</button></div></form><section class="card border-0 shadow-sm" aria-labelledby="members-heading"><div class="card-header bg-white border-bottom"><h2 id="members-heading" class="h5 mb-0">Membership directory</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Member</th><th>Type</th><th>Membership period</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($members as $member)<tr><td><strong>{{ $member->user->name ?? 'Unknown' }}</strong><span class="d-block small text-muted">{{ $member->user->email ?? 'Email not recorded' }}</span></td><td>{{ Str::title($member->membership_type) }}</td><td>{{ $member->membership_start_date?->format('M j, Y') }} - {{ $member->membership_end_date?->format('M j, Y') ?? 'No expiry' }}</td><td><span class="badge {{ $member->is_active ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $member->is_active ? 'Active' : 'Inactive' }}</span></td><td>@if($member->is_active)<form method="POST" action="{{ route('library.members.deactivate', $member) }}">@csrf<button class="btn btn-sm btn-outline-danger" type="submit">Deactivate</button></form>@else<form method="POST" action="{{ route('library.members.activate', $member) }}">@csrf<button class="btn btn-sm btn-outline-success" type="submit">Activate</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No library members found.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $members->links() }}</div></section></div>
@endsection
