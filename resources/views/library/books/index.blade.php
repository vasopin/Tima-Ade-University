@extends('layouts.app')

@section('title', 'Book Catalog')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4"><div><p class="text-uppercase text-muted small fw-semibold mb-1">Library workspace</p><h1 class="h2 mb-1">Book Catalog</h1><p class="text-muted mb-0">Search and manage the physical and bibliographic catalog.</p></div><a href="{{ route('library.books.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-1" aria-hidden="true"></i>Add book</a></div><form class="card border-0 shadow-sm mb-4" method="GET"><div class="card-body d-flex gap-2"><label for="book-search" class="visually-hidden">Search catalog</label><input id="book-search" name="search" value="{{ request('search') }}" class="form-control" placeholder="Search title, author, or ISBN"><button class="btn btn-outline-primary" type="submit">Search</button></div></form><section class="card border-0 shadow-sm" aria-labelledby="catalog-heading"><div class="card-header bg-white border-bottom"><h2 id="catalog-heading" class="h5 mb-0">Catalog items</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Title</th><th>Author / ISBN</th><th>Category</th><th>Copies</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($books as $book)<tr><td><strong>{{ $book->title }}</strong><span class="d-block small text-muted">{{ $book->publisher ?? 'Publisher not recorded' }}</span></td><td>{{ $book->author ?? 'Not recorded' }}<span class="d-block small text-muted">{{ $book->isbn ?? 'ISBN not recorded' }}</span></td><td>{{ $book->category ?? 'Uncategorized' }}</td><td>{{ $book->available_copies }} / {{ $book->total_copies }} available</td><td><span class="badge {{ $book->is_available ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $book->is_available ? 'Available' : 'Unavailable' }}</span></td><td><a href="{{ route('library.books.edit', $book) }}" class="btn btn-sm btn-outline-secondary">Edit</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No catalog items match this search.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $books->links() }}</div></section></div>
@endsection
