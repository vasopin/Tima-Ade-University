@extends('layouts.app')

@section('title', 'Edit Book')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Library workspace</p><h1 class="h2 mb-1">Edit Book</h1><p class="text-muted mb-0">Update catalog details for {{ $book->title }}.</p></div><section class="card border-0 shadow-sm"><form method="POST" action="{{ route('library.books.update', $book) }}"><div class="card-body row g-3">@csrf @method('PUT')<div class="col-12 col-md-8"><label for="book-title" class="form-label">Title</label><input id="book-title" name="title" value="{{ old('title', $book->title) }}" class="form-control" required></div><div class="col-12 col-md-4"><label for="book-isbn" class="form-label">ISBN</label><input id="book-isbn" name="isbn" value="{{ old('isbn', $book->isbn) }}" class="form-control"></div><div class="col-12 col-md-6"><label for="book-author" class="form-label">Author</label><input id="book-author" name="author" value="{{ old('author', $book->author) }}" class="form-control"></div><div class="col-12 col-md-6"><label for="book-category" class="form-label">Category</label><input id="book-category" name="category" value="{{ old('category', $book->category) }}" class="form-control"></div><div class="col-12 col-md-6"><label for="book-publisher" class="form-label">Publisher</label><input id="book-publisher" name="publisher" value="{{ old('publisher', $book->publisher) }}" class="form-control"></div><div class="col-12 col-md-6"><label for="book-year" class="form-label">Publication year</label><input id="book-year" name="publication_year" type="number" min="1500" max="2100" value="{{ old('publication_year', $book->publication_year) }}" class="form-control"></div><div class="col-12 col-md-4"><label for="book-copies" class="form-label">Total copies</label><input id="book-copies" name="total_copies" type="number" min="1" value="{{ old('total_copies', $book->total_copies) }}" class="form-control" required></div><div class="col-12"><label for="book-description" class="form-label">Description</label><textarea id="book-description" name="description" rows="3" class="form-control">{{ old('description', $book->description) }}</textarea></div></div><div class="card-footer bg-white"><button class="btn btn-primary" type="submit">Save catalog entry</button></div></form></section></div>
@endsection
