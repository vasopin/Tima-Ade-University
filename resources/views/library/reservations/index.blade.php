@extends('layouts.app')

@section('title', 'Reservations')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Library workspace</p><h1 class="h2 mb-1">Reservations</h1><p class="text-muted mb-0">Manage member reservations and fulfil available-book requests.</p></div><section class="card border-0 shadow-sm mb-4"><div class="card-header bg-white border-bottom"><h2 class="h5 mb-0">Create reservation</h2></div><form method="POST" action="{{ route('library.reservations.store') }}"><div class="card-body row g-3">@csrf<div class="col-12 col-md-6"><label for="reservation-member" class="form-label">Active member</label><select id="reservation-member" name="library_member_id" class="form-select" required><option value="">Select member</option>@foreach($activeMembers as $member)<option value="{{ $member->id }}">{{ $member->user->name ?? 'Unknown' }}</option>@endforeach</select></div><div class="col-12 col-md-6"><label for="reservation-book" class="form-label">Book</label><select id="reservation-book" name="book_id" class="form-select" required><option value="">Select book</option>@foreach($books as $book)<option value="{{ $book->id }}">{{ $book->title }}</option>@endforeach</select></div></div><div class="card-footer bg-white"><button class="btn btn-primary" type="submit">Create reservation</button></div></form></section><section class="card border-0 shadow-sm" aria-labelledby="reservations-heading"><div class="card-header bg-white border-bottom"><h2 id="reservations-heading" class="h5 mb-0">Reservation ledger</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Member</th><th>Book</th><th>Reserved</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($reservations as $reservation)<tr><td>{{ $reservation->libraryMember->user->name ?? 'Unknown' }}</td><td>{{ $reservation->book->title ?? 'Unknown' }}</td><td>{{ $reservation->reservation_date?->format('M j, Y') }}</td><td>{{ Str::title($reservation->status) }}</td><td>@if($reservation->status === 'active')<form method="POST" action="{{ route('library.reservations.fulfil', $reservation) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-success" type="submit">Fulfil</button></form><form method="POST" action="{{ route('library.reservations.cancel', $reservation) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-danger" type="submit">Cancel</button></form>@else<span class="small text-muted">Complete</span>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No reservations available.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $reservations->links() }}</div></section></div>
@endsection
