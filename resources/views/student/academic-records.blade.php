@extends('layouts.app')

@section('title', 'Academic Records')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Student portal</p><h1 class="h2 mb-1">Academic Records &amp; Grades</h1><p class="text-muted mb-0">Your recorded examination history and generated transcripts.</p></div><div class="row g-3 mb-4"><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Cumulative GPA</span><h2 class="h4 mt-2 mb-0">{{ number_format($cumulativeGpa, 2) }}</h2></div></div></div><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Recorded assessments</span><h2 class="h4 mt-2 mb-0">{{ $marks->count() }}</h2></div></div></div><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Transcripts</span><h2 class="h4 mt-2 mb-0">{{ $transcripts->count() }}</h2></div></div></div></div><section class="card border-0 shadow-sm" aria-labelledby="grades-heading"><div class="card-header bg-white border-bottom"><h2 id="grades-heading" class="h5 mb-0">Grade history</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Subject</th><th>Assessment</th><th>Score</th><th>Grade</th><th>Date</th></tr></thead><tbody>@forelse($marks as $mark)<tr><td>{{ $mark->exam->subject->name ?? 'General' }}</td><td>{{ $mark->exam->name }}</td><td>{{ $mark->is_absent ? 'Absent' : $mark->marks_obtained . ' / ' . $mark->exam->total_marks }}</td><td>{{ $mark->is_absent ? 'F' : $mark->grade }}</td><td>{{ $mark->exam->exam_date?->format('M j, Y') }}</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No examination results are available.</td></tr>@endforelse</tbody></table></div></section></div>
@endsection
