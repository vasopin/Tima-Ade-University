@extends('layouts.app')

@section('title', 'Fees and Payments')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Student portal</p><h1 class="h2 mb-1">Fee Invoices &amp; Payments</h1><p class="text-muted mb-0">Your fee payment history and outstanding balance.</p></div><div class="card border-0 shadow-sm mb-4"><div class="card-body"><span class="small text-muted">Outstanding balance</span><h2 class="h3 mt-2 mb-0">₦{{ number_format($outstanding, 2) }}</h2></div></div>@if($invoices->isNotEmpty())<section class="card border-0 shadow-sm mb-4"><div class="card-header bg-white border-bottom"><h2 class="h5 mb-0">Invoices</h2></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Invoice</th><th>Due date</th><th>Total</th><th>Balance</th><th></th></tr></thead><tbody>@foreach($invoices as $invoice)<tr><td>{{ $invoice->invoice_number }}</td><td>{{ $invoice->due_date?->format('M j, Y') ?? '—' }}</td><td>₦{{ number_format($invoice->total, 2) }}</td><td class="text-danger">₦{{ number_format($invoice->balance, 2) }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('fees.show-invoice', $invoice) }}">View invoice</a></td></tr>@endforeach</tbody></table></div></section>@endif<section class="card border-0 shadow-sm" aria-labelledby="fees-heading"><div class="card-header bg-white border-bottom"><h2 id="fees-heading" class="h5 mb-0">Payment history</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Receipt</th><th>Fee</th><th>Amount paid</th><th>Date</th><th>Status</th><th>Details</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ $payment->receipt_number }}</td><td>{{ Str::title(str_replace('_', ' ', $payment->feeStructure->fee_type ?? 'Fee')) }}</td><td>₦{{ number_format($payment->amount_paid, 2) }}</td><td>{{ $payment->payment_date?->format('M j, Y') }}</td><td>{{ Str::title($payment->status) }}</td><td><a href="{{ route('fees.show', $payment) }}" class="btn btn-sm btn-outline-primary">View receipt</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No fee payment records are available.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $payments->links() }}</div></section>@if($awards->isNotEmpty())<p class="small text-muted mt-3">Approved scholarships: ₦{{ number_format($awards->sum('amount'), 2) }}</p>@endif</div>
@endsection
