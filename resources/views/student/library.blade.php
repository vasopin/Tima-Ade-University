@extends('layouts.app')

@section('title', 'Library Account')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Student portal</p><h1 class="h2 mb-1">Library Account</h1><p class="text-muted mb-0">Borrowed materials and membership status.</p></div><div class="card border-0 shadow-sm mb-4"><div class="card-body"><span class="small text-muted">Membership</span><h2 class="h5 mt-2 mb-0">{{ $member?->is_active ? 'Active' : 'No active membership' }}</h2>@if($member)<p class="small text-muted mb-0">{{ Str::title($member->membership_type) }} · Expires {{ $member->membership_end_date?->format('M j, Y') ?? 'Not specified' }}</p>@endif</div></div><section class="card border-0 shadow-sm" aria-labelledby="borrowed-heading"><div class="card-header bg-white border-bottom"><h2 id="borrowed-heading" class="h5 mb-0">Borrowed books</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Title</th><th>Borrowed</th><th>Due date</th><th>Status</th><th>Late days</th></tr></thead><tbody>@forelse($member?->borrowings?->whereIn('status', ['active', 'overdue']) ?? collect() as $borrowing)<tr><td>{{ $borrowing->book->title ?? 'Unknown book' }}</td><td>{{ $borrowing->borrow_date?->format('M j, Y') }}</td><td>{{ $borrowing->due_date?->format('M j, Y') }}</td><td>{{ Str::title($borrowing->status) }}</td><td>{{ $borrowing->due_date && $borrowing->due_date->isPast() ? $borrowing->due_date->diffInDays(today()) : 0 }}</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No active borrowed books.</td></tr>@endforelse</tbody></table></div></section><p class="small text-muted mt-3">Fine payments and saved e-resource collections are not configured in the current library schema.</p></div>
@endsection
