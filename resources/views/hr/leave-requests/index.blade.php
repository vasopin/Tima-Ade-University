@extends('layouts.app')

@section('title', 'Leave Requests')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">HR workspace</p><h1 class="h2 mb-1">Leave Requests</h1><p class="text-muted mb-0">Review staff leave applications and approval status.</p></div><div class="row g-3 mb-4"><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Pending</span><h2 class="h4 mt-2 mb-0">{{ $leaveRequests->where('status', 'pending')->count() }}</h2></div></div></div><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Approved on this page</span><h2 class="h4 mt-2 mb-0">{{ $leaveRequests->where('status', 'approved')->count() }}</h2></div></div></div><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Rejected on this page</span><h2 class="h4 mt-2 mb-0">{{ $leaveRequests->where('status', 'rejected')->count() }}</h2></div></div></div></div><form class="card border-0 shadow-sm mb-4" method="GET"><div class="card-body row g-2 align-items-end"><div class="col-12 col-md-4"><label for="leave-status" class="form-label">Status</label><select id="leave-status" name="status" class="form-select"><option value="">All statuses</option>@foreach(['pending' => 'Pending', 'approved' => 'Approved', 'rejected' => 'Rejected'] as $value => $label)<option value="{{ $value }}" @selected(request('status') === $value)>{{ $label }}</option>@endforeach</select></div><div class="col-12 col-md-2"><button class="btn btn-primary w-100" type="submit">Filter</button></div></div></form><section class="card border-0 shadow-sm" aria-labelledby="leave-heading"><div class="card-header bg-white border-bottom"><h2 id="leave-heading" class="h5 mb-0">Staff leave applications</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Employee</th><th>Leave type</th><th>Dates</th><th>Days / allowance</th><th>Status</th><th>Decision</th></tr></thead><tbody>@forelse($leaveRequests as $leave)<tr><td>{{ $leave->employee->user->name ?? 'Unknown employee' }}<span class="d-block small text-muted">{{ $leave->employee->employee_id ?? '' }}</span></td><td>{{ $leave->leaveType->name ?? 'Unknown' }}</td><td>{{ $leave->from_date?->format('M j, Y') }} - {{ $leave->to_date?->format('M j, Y') }}</td><td>{{ $leave->number_of_days }} / {{ $leave->leaveType->days_allowed ?? 'Not set' }}</td><td><span class="badge {{ $leave->status === 'approved' ? 'text-bg-success' : ($leave->status === 'rejected' ? 'text-bg-danger' : 'text-bg-warning') }}">{{ Str::title($leave->status) }}</span></td><td>@if($leave->status === 'pending')<form method="POST" action="{{ route('hr.leave-requests.approve', $leave) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-success" type="submit">Approve</button></form><form method="POST" action="{{ route('hr.leave-requests.reject', $leave) }}" class="d-inline-flex gap-1 mt-1 mt-md-0">@csrf<input name="approval_notes" aria-label="Rejection reason" required class="form-control form-control-sm" placeholder="Reason"><button class="btn btn-sm btn-outline-danger" type="submit">Reject</button></form>@else<span class="small text-muted">{{ $leave->approvedBy->name ?? 'Processed' }}</span>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No leave requests match this filter.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $leaveRequests->links() }}</div></section></div>
@endsection
