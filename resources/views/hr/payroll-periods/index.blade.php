@extends('layouts.app')

@section('title', 'Payroll Periods')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4"><div><p class="text-uppercase text-muted small fw-semibold mb-1">HR workspace</p><h1 class="h2 mb-1">Payroll Periods</h1><p class="text-muted mb-0">Open and close payroll periods used by staff payment records.</p></div></div><section class="card border-0 shadow-sm mb-4"><div class="card-header bg-white border-bottom"><h2 class="h5 mb-0">Create period</h2></div><form method="POST" action="{{ route('hr.payroll-periods.store') }}"><div class="card-body row g-3">@csrf<div class="col-12 col-md-4"><label for="period-name" class="form-label">Period name</label><input id="period-name" name="period_name" class="form-control" required></div><div class="col-12 col-md-4"><label for="period-start" class="form-label">Start date</label><input id="period-start" type="date" name="start_date" class="form-control" required></div><div class="col-12 col-md-4"><label for="period-end" class="form-label">End date</label><input id="period-end" type="date" name="end_date" class="form-control" required></div></div><div class="card-footer bg-white"><button class="btn btn-primary" type="submit">Create payroll period</button></div></form></section><section class="card border-0 shadow-sm"><div class="card-header bg-white border-bottom"><h2 class="h5 mb-0">Periods</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Period</th><th>Dates</th><th>Records</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($periods as $period)<tr><td>{{ $period->period_name }}</td><td>{{ $period->start_date?->format('M j, Y') }} - {{ $period->end_date?->format('M j, Y') }}</td><td>{{ $period->payroll_records_count }}</td><td>{{ $period->is_closed ? 'Closed' : ($period->is_active ? 'Active' : 'Inactive') }}</td><td>@if(!$period->is_closed)<form method="POST" action="{{ route('hr.payroll-periods.close', $period) }}">@csrf<button class="btn btn-sm btn-outline-danger" type="submit">Close</button></form>@else<span class="small text-muted">Closed</span>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No payroll periods available.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $periods->links() }}</div></section></div>
@endsection
