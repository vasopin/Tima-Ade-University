@extends('layouts.app')

@section('title', 'Payroll')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">HR workspace</p><h1 class="h2 mb-1">Payroll</h1><p class="text-muted mb-0">Generate payroll from recorded salaries and process staff payment records.</p></div><div class="row g-3 mb-4"><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Records on this page</span><h2 class="h4 mt-2 mb-0">{{ $records->total() }}</h2></div></div></div><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Pending on this page</span><h2 class="h4 mt-2 mb-0">{{ $records->where('status', 'pending')->count() }}</h2></div></div></div><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Paid on this page</span><h2 class="h4 mt-2 mb-0">{{ $records->where('status', 'paid')->count() }}</h2></div></div></div></div><section class="card border-0 shadow-sm mb-4"><div class="card-header bg-white border-bottom"><h2 class="h5 mb-0">Generate payroll record</h2></div><form method="POST" action="{{ route('hr.payroll-records.store') }}"><div class="card-body row g-3">@csrf<div class="col-12 col-lg-5"><label for="payroll-employee" class="form-label">Employee</label><select id="payroll-employee" name="employee_id" class="form-select" required><option value="">Select employee</option>@foreach($allEmployees as $employee)<option value="{{ $employee->id }}">{{ $employee->user->name ?? $employee->employee_id }} - {{ $employee->employee_id }}</option>@endforeach</select></div><div class="col-12 col-lg-4"><label for="payroll-period" class="form-label">Payroll period</label><select id="payroll-period" name="payroll_period_id" class="form-select" required><option value="">Select period</option>@foreach($periods as $period)<option value="{{ $period->id }}">{{ $period->period_name }}{{ $period->is_closed ? ' (closed)' : '' }}</option>@endforeach</select></div><div class="col-12 col-lg-3"><label for="deductions" class="form-label">Deductions</label><input id="deductions" type="number" min="0" step="0.01" name="total_deductions" class="form-control" value="0"></div><div class="col-12"><label for="payroll-notes" class="form-label">Notes</label><textarea id="payroll-notes" name="notes" rows="2" class="form-control"></textarea></div></div><div class="card-footer bg-white"><button class="btn btn-primary" type="submit">Generate from base salary</button></div></form></section><section class="card border-0 shadow-sm" aria-labelledby="payroll-history-heading"><div class="card-header bg-white border-bottom d-flex justify-content-between align-items-center"><h2 id="payroll-history-heading" class="h5 mb-0">Payroll history</h2><a href="{{ route('hr.payroll-periods.index') }}" class="btn btn-sm btn-outline-secondary">Manage periods</a></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Employee</th><th>Period</th><th>Gross</th><th>Deductions</th><th>Net</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{ $record->employee->user->name ?? 'Unknown' }}</td><td>{{ $record->payrollPeriod->period_name ?? 'Unknown' }}</td><td>₦{{ number_format($record->gross_salary, 2) }}</td><td>₦{{ number_format($record->total_deductions, 2) }}</td><td>₦{{ number_format($record->net_salary, 2) }}</td><td><span class="badge {{ $record->status === 'paid' ? 'text-bg-success' : ($record->status === 'processed' ? 'text-bg-info' : 'text-bg-warning') }}">{{ Str::title($record->status) }}</span></td><td>@if($record->status === 'pending')<form method="POST" action="{{ route('hr.payroll-records.process', $record) }}">@csrf<button class="btn btn-sm btn-outline-primary" type="submit">Process</button></form>@elseif($record->status === 'processed')<form method="POST" action="{{ route('hr.payroll-records.mark-paid', $record) }}">@csrf<button class="btn btn-sm btn-outline-success" type="submit">Mark paid</button></form>@else<span class="small text-muted">Complete</span>@endif</td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">No payroll records available.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $records->links() }}</div></section></div>
@endsection
