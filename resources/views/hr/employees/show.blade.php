@extends('layouts.app')

@section('title', 'Employee Profile')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="d-flex flex-column flex-md-row justify-content-between gap-3 mb-4"><div><p class="text-uppercase text-muted small fw-semibold mb-1">HR workspace</p><h1 class="h2 mb-1">{{ $employee->user->name ?? 'Employee profile' }}</h1><p class="text-muted mb-0">{{ $employee->employee_id }}</p></div><a href="{{ route('hr.employees.edit', $employee) }}" class="btn btn-outline-primary align-self-start">Edit profile</a></div><div class="row g-4"><div class="col-12 col-lg-5"><section class="card border-0 shadow-sm"><div class="card-header bg-white border-bottom"><h2 class="h5 mb-0">Employment details</h2></div><dl class="card-body row mb-0"><dt class="col-6 text-muted">Department</dt><dd class="col-6">{{ $employee->department ?? 'Not assigned' }}</dd><dt class="col-6 text-muted">Designation</dt><dd class="col-6">{{ $employee->designation ?? 'Not specified' }}</dd><dt class="col-6 text-muted">Type</dt><dd class="col-6">{{ Str::title(str_replace('_', ' ', $employee->employment_type)) }}</dd><dt class="col-6 text-muted">Joined</dt><dd class="col-6">{{ $employee->date_of_joining?->format('M j, Y') ?? 'Not recorded' }}</dd><dt class="col-6 text-muted">Email</dt><dd class="col-6 text-break">{{ $employee->user->email ?? 'Not recorded' }}</dd></dl></section></div><div class="col-12 col-lg-7"><section class="card border-0 shadow-sm"><div class="card-header bg-white border-bottom"><h2 class="h5 mb-0">Leave and payroll history</h2></div><div class="card-body"><p class="mb-2"><strong>{{ $employee->leaveRequests->count() }}</strong> leave request(s)</p><p class="mb-0"><strong>{{ $employee->payrollRecords->count() }}</strong> payroll record(s)</p></div></section></div></div></div>
@endsection
