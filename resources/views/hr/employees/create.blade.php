@extends('layouts.app')

@section('title', 'Add Employee')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">HR workspace</p><h1 class="h2 mb-1">Add Employee</h1><p class="text-muted mb-0">Link an existing non-student user account to a staff record.</p></div><section class="card border-0 shadow-sm"><form method="POST" action="{{ route('hr.employees.store') }}"><div class="card-body row g-3">@csrf<div class="col-12"><label for="user-id" class="form-label">User account</label><select id="user-id" name="user_id" class="form-select" required><option value="">Select a user</option>@foreach($users as $user)<option value="{{ $user->id }}" @selected(old('user_id') == $user->id)>{{ $user->name }} ({{ $user->email }})</option>@endforeach</select></div><div class="col-12 col-md-6"><label for="department" class="form-label">Department</label><input id="department" name="department" value="{{ old('department') }}" class="form-control"></div><div class="col-12 col-md-6"><label for="designation" class="form-label">Job role / designation</label><input id="designation" name="designation" value="{{ old('designation') }}" class="form-control"></div><div class="col-12 col-md-6"><label for="date-of-joining" class="form-label">Date of joining</label><input id="date-of-joining" type="date" name="date_of_joining" value="{{ old('date_of_joining') }}" class="form-control"></div><div class="col-12 col-md-6"><label for="employment-type" class="form-label">Employment type</label><select id="employment-type" name="employment_type" class="form-select" required>@foreach(['full_time' => 'Full time', 'part_time' => 'Part time', 'contract' => 'Contract', 'temporary' => 'Temporary'] as $value => $label)<option value="{{ $value }}" @selected(old('employment_type', 'full_time') === $value)>{{ $label }}</option>@endforeach</select></div><div class="col-12 col-md-6"><label for="base-salary" class="form-label">Base salary</label><input id="base-salary" type="number" min="0" step="0.01" name="base_salary" value="{{ old('base_salary') }}" class="form-control"></div><div class="col-12"><label for="employee-notes" class="form-label">Notes</label><textarea id="employee-notes" name="notes" rows="4" class="form-control">{{ old('notes') }}</textarea></div></div><div class="card-footer bg-white"><button class="btn btn-primary" type="submit">Create employee record</button></div></form></section></div>
@endsection
