@extends('layouts.app')

@section('title', 'Employees')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4"><div><p class="text-uppercase text-muted small fw-semibold mb-1">HR workspace</p><h1 class="h2 mb-1">Employees</h1><p class="text-muted mb-0">Staff profiles, departments, employment details, and contact access.</p></div><a href="{{ route('hr.employees.create') }}" class="btn btn-primary"><i class="bi bi-person-plus me-1" aria-hidden="true"></i>Add employee</a></div><form class="card border-0 shadow-sm mb-4" method="GET"><div class="card-body d-flex gap-2"><label for="employee-search" class="visually-hidden">Search employees</label><input id="employee-search" name="search" value="{{ request('search') }}" class="form-control" placeholder="Search by name or employee ID"><button class="btn btn-outline-primary" type="submit">Search</button></div></form><section class="card border-0 shadow-sm" aria-labelledby="employees-heading"><div class="card-header bg-white border-bottom"><h2 id="employees-heading" class="h5 mb-0">Staff directory</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Employee</th><th>Contact</th><th>Department</th><th>Role</th><th>Employment</th><th>Action</th></tr></thead><tbody>@forelse($employees as $employee)<tr><td><a href="{{ route('hr.employees.show', $employee) }}" class="fw-semibold">{{ $employee->user->name ?? 'Unknown' }}</a><span class="d-block small text-muted">{{ $employee->employee_id }}</span></td><td>{{ $employee->user->email ?? 'Not recorded' }}<span class="d-block small text-muted">{{ $employee->user->phone ?? 'Phone not recorded' }}</span></td><td>{{ $employee->department ?? 'Not assigned' }}</td><td>{{ $employee->designation ?? 'Not specified' }}</td><td>{{ Str::title(str_replace('_', ' ', $employee->employment_type)) }}</td><td><a class="btn btn-sm btn-outline-secondary" href="{{ route('hr.employees.edit', $employee) }}">Edit</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No employee records available.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $employees->links() }}</div></section></div>
@endsection
