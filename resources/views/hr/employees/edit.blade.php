@extends('layouts.app')

@section('title', 'Edit Employee')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">HR workspace</p><h1 class="h2 mb-1">Edit Employee</h1><p class="text-muted mb-0">{{ $employee->user->name ?? 'Employee' }} · {{ $employee->employee_id }}</p></div><section class="card border-0 shadow-sm"><form method="POST" action="{{ route('hr.employees.update', $employee) }}"><div class="card-body row g-3">@csrf @method('PUT')<div class="col-12 col-md-6"><label for="department" class="form-label">Department</label><input id="department" name="department" value="{{ old('department', $employee->department) }}" class="form-control"></div><div class="col-12 col-md-6"><label for="designation" class="form-label">Job role / designation</label><input id="designation" name="designation" value="{{ old('designation', $employee->designation) }}" class="form-control"></div><div class="col-12 col-md-6"><label for="date-of-joining" class="form-label">Date of joining</label><input id="date-of-joining" type="date" name="date_of_joining" value="{{ old('date_of_joining', $employee->date_of_joining?->format('Y-m-d')) }}" class="form-control"></div><div class="col-12 col-md-6"><label for="date-of-leaving" class="form-label">Date of leaving</label><input id="date-of-leaving" type="date" name="date_of_leaving" value="{{ old('date_of_leaving', $employee->date_of_leaving?->format('Y-m-d')) }}" class="form-control"></div><div class="col-12 col-md-6"><label for="employment-type" class="form-label">Employment type</label><select id="employment-type" name="employment_type" class="form-select" required>@foreach(['full_time' => 'Full time', 'part_time' => 'Part time', 'contract' => 'Contract', 'temporary' => 'Temporary'] as $value => $label)<option value="{{ $value }}" @selected(old('employment_type', $employee->employment_type) === $value)>{{ $label }}</option>@endforeach</select></div><div class="col-12 col-md-6"><label for="base-salary" class="form-label">Base salary</label><input id="base-salary" type="number" min="0" step="0.01" name="base_salary" value="{{ old('base_salary', $employee->base_salary) }}" class="form-control"></div><div class="col-12"><label for="employee-notes" class="form-label">Notes</label><textarea id="employee-notes" name="notes" rows="4" class="form-control">{{ old('notes', $employee->notes) }}</textarea></div></div><div class="card-footer bg-white"><button class="btn btn-primary" type="submit">Save employee record</button></div></form></section></div>
@endsection
