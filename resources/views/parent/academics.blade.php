@extends('layouts.app')

@section('title', 'Academic Progress')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Parent portal</p><h1 class="h2 mb-1">Academic Progress &amp; Grades</h1><p class="text-muted mb-0">Read-only academic results for the selected linked student.</p></div>@if($children->isEmpty())<div class="card border-0 shadow-sm"><div class="card-body text-center p-5"><h2 class="h5">No linked students</h2></div></div>@else<div class="d-flex flex-wrap gap-2 mb-4" role="group" aria-label="Select linked student">@foreach($children as $child)<a href="{{ route('parent.academics', ['student_id' => $child->id]) }}" class="btn {{ $selectedChild?->id === $child->id ? 'btn-primary' : 'btn-outline-secondary' }}">{{ $child->user->name }}</a>@endforeach</div><div class="row g-3 mb-4"><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Cumulative GPA</span><h2 class="h4 mt-2 mb-0">{{ number_format($gpa, 2) }}</h2></div></div></div><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Academic standing</span><h2 class="h5 mt-2 mb-0">{{ Str::title($standing?->status ?? 'Not assessed') }}</h2></div></div></div><div class="col-12 col-md-4"><div class="card border-0 shadow-sm"><div class="card-body"><span class="small text-muted">Recorded grades</span><h2 class="h4 mt-2 mb-0">{{ $marks->count() }}</h2></div></div></div></div><section class="card border-0 shadow-sm" aria-labelledby="parent-grades-heading"><div class="card-header bg-white border-bottom"><h2 id="parent-grades-heading" class="h5 mb-0">Grade history</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Subject</th><th>Assessment</th><th>Score</th><th>Grade</th></tr></thead><tbody>@forelse($marks as $mark)<tr><td>{{ $mark->exam->subject->name ?? 'General' }}</td><td>{{ $mark->exam->name }}</td><td>{{ $mark->is_absent ? 'Absent' : $mark->marks_obtained . ' / ' . $mark->exam->total_marks }}</td><td>{{ $mark->is_absent ? 'F' : $mark->grade }}</td></tr>@empty<tr><td colspan="4" class="text-center text-muted py-5">No grades available.</td></tr>@endforelse</tbody></table></div></section>@endif</div>
@endsection
