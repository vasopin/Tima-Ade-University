@extends('layouts.app')

@section('title', 'Financial Oversight')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Parent portal</p><h1 class="h2 mb-1">Financial Oversight &amp; Payments</h1><p class="text-muted mb-0">Read-only fee history for the selected linked student.</p></div>@if($children->isEmpty())<div class="card border-0 shadow-sm"><div class="card-body text-center p-5"><h2 class="h5">No linked students</h2></div></div>@else<div class="d-flex flex-wrap gap-2 mb-4" role="group" aria-label="Select linked student">@foreach($children as $child)<a href="{{ route('parent.finance', ['student_id' => $child->id]) }}" class="btn {{ $selectedChild?->id === $child->id ? 'btn-primary' : 'btn-outline-secondary' }}">{{ $child->user->name }}</a>@endforeach</div><div class="card border-0 shadow-sm mb-4"><div class="card-body"><span class="small text-muted">Outstanding balance</span><h2 class="h3 mt-2 mb-0">₦{{ number_format($outstanding, 2) }}</h2></div></div><section class="card border-0 shadow-sm" aria-labelledby="parent-fees-heading"><div class="card-header bg-white border-bottom"><h2 id="parent-fees-heading" class="h5 mb-0">Itemized payment history</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Receipt</th><th>Fee</th><th>Amount paid</th><th>Status</th><th>Date</th><th>Details</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ $payment->receipt_number }}</td><td>{{ Str::title(str_replace('_', ' ', $payment->feeStructure->fee_type ?? 'Fee')) }}</td><td>₦{{ number_format($payment->amount_paid, 2) }}</td><td>{{ Str::title($payment->status) }}</td><td>{{ $payment->payment_date?->format('M j, Y') }}</td><td><a href="{{ route('fees.show', $payment) }}" class="btn btn-sm btn-outline-primary">View receipt</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No fee records available.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $payments->links() }}</div></section><p class="small text-muted mt-3">Online payment gateways and invoice downloads are not configured in the current fee system.</p>@endif</div>
@endsection
