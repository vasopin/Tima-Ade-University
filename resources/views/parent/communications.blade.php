@extends('layouts.app')

@section('title', 'University Communications')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Parent portal</p><h1 class="h2 mb-1">University Communications</h1><p class="text-muted mb-0">Official notices, alerts, and deadline reminders.</p></div><div class="row g-4"><div class="col-12 col-lg-7"><section class="card border-0 shadow-sm" aria-labelledby="parent-notifications-heading"><div class="card-header bg-white border-bottom"><h2 id="parent-notifications-heading" class="h5 mb-0">Notifications</h2></div>@forelse($notifications as $notification)<article class="border-bottom p-3"><h3 class="h6 mb-1">{{ $notification->title }}</h3><p class="mb-1 text-secondary">{{ $notification->body }}</p><time class="small text-muted">{{ $notification->created_at?->format('M j, Y, g:i A') }}</time></article>@empty<div class="p-5 text-center text-muted">No notifications available.</div>@endforelse<div class="p-3">{{ $notifications->links() }}</div></section></div><div class="col-12 col-lg-5"><section class="card border-0 shadow-sm" aria-labelledby="parent-notices-heading"><div class="card-header bg-white border-bottom"><h2 id="parent-notices-heading" class="h5 mb-0">Official notices</h2></div>@forelse($notices as $notice)<a href="{{ route('public.notices.single', $notice) }}" class="d-block border-bottom p-3 text-decoration-none text-body"><strong>{{ $notice->title }}</strong><span class="d-block small text-muted">{{ $notice->published_date?->format('M j, Y') }}</span></a>@empty<div class="p-4 text-center text-muted">No official notices available.</div>@endforelse</section></div></div></div>
@endsection
