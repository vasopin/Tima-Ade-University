@extends('layouts.app')

@section('title', 'Student Profiles')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="mb-4"><p class="text-uppercase text-muted small fw-semibold mb-1">Parent portal</p><h1 class="h2 mb-1">Linked Student Profiles</h1><p class="text-muted mb-0">Read-only details for students linked to your account.</p></div>@if($children->isEmpty())<div class="card border-0 shadow-sm"><div class="card-body text-center p-5"><h2 class="h5">No linked students</h2><p class="text-muted mb-0">Contact university administration to link a student record.</p></div></div>@else<div class="d-flex flex-wrap gap-2 mb-4" role="group" aria-label="Select linked student">@foreach($children as $child)<a href="{{ route('parent.profiles', ['student_id' => $child->id]) }}" class="btn {{ $selectedChild?->id === $child->id ? 'btn-primary' : 'btn-outline-secondary' }}">{{ $child->user->name }}</a>@endforeach</div><section class="card border-0 shadow-sm" aria-labelledby="profile-heading"><div class="card-header bg-white border-bottom"><h2 id="profile-heading" class="h5 mb-0">{{ $selectedChild->user->name }}</h2></div><dl class="card-body row mb-0"><dt class="col-12 col-md-4 text-muted">Student ID</dt><dd class="col-12 col-md-8">{{ $selectedChild->student_id }}</dd><dt class="col-12 col-md-4 text-muted">Status</dt><dd class="col-12 col-md-8">{{ Str::title($selectedChild->status) }}</dd><dt class="col-12 col-md-4 text-muted">Class / programme</dt><dd class="col-12 col-md-8">{{ $selectedChild->schoolClass->name ?? 'Not recorded' }}</dd><dt class="col-12 col-md-4 text-muted">Section</dt><dd class="col-12 col-md-8">{{ $selectedChild->section->name ?? 'Not recorded' }}</dd><dt class="col-12 col-md-4 text-muted">Email</dt><dd class="col-12 col-md-8 text-break">{{ $selectedChild->user->email ?? 'Not recorded' }}</dd></dl></section>@endif</div>
@endsection
