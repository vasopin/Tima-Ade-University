@extends('layouts.app')

@section('title', 'Graduation Clearance')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4"><div><p class="text-uppercase text-muted small fw-semibold mb-1">Registrar workspace</p><h1 class="h2 mb-1">Graduation Clearance</h1><p class="text-muted mb-0">Track academic eligibility and final graduation approvals.</p></div><a href="{{ route('registrar.graduation.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-1" aria-hidden="true"></i>Start clearance</a></div><section class="card border-0 shadow-sm" aria-labelledby="graduation-heading"><div class="card-header bg-white border-bottom"><h2 id="graduation-heading" class="h5 mb-0">Clearance records</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Student</th><th>Degree</th><th>Academic eligibility</th><th>Approval</th><th>Action</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{ $record->student->user->name ?? 'Unknown student' }}</td><td>{{ $record->degree_name ?? 'Not specified' }}</td><td>{{ Str::title(str_replace('_', ' ', $record->status)) }}</td><td>{{ $record->approved_at?->format('M j, Y') ?? 'Pending' }}</td><td>@if($record->status === 'eligible')<form method="POST" action="{{ route('registrar.graduation.approve', $record) }}">@csrf<button class="btn btn-sm btn-outline-success" type="submit">Approve</button></form>@else<span class="small text-muted">No action</span>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No graduation clearance records available.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $records->links() }}</div></section></div>
@endsection
