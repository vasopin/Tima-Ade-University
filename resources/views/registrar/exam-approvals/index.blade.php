@extends('layouts.app')

@section('title', 'Exam Approvals')

@section('content')
<div class="container-fluid px-4 py-4 bg-light min-vh-100"><div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4"><div><p class="text-uppercase text-muted small fw-semibold mb-1">Registrar workspace</p><h1 class="h2 mb-1">Exam Approvals</h1><p class="text-muted mb-0">Review completed examinations, recorded marks, and schedule conflicts before approval.</p></div></div><form class="card border-0 shadow-sm mb-4" method="GET"><div class="card-body row g-2 align-items-end"><div class="col-12 col-md-4"><label for="approval-status" class="form-label">Review status</label><select id="approval-status" name="status" class="form-select"><option value="">All completed exams</option><option value="pending" @selected(request('status') === 'pending')>Pending</option><option value="approved" @selected(request('status') === 'approved')>Approved</option><option value="rejected" @selected(request('status') === 'rejected')>Rejected</option></select></div><div class="col-12 col-md-2"><button class="btn btn-primary w-100" type="submit">Filter</button></div></div></form><section class="card border-0 shadow-sm" aria-labelledby="exam-approval-heading"><div class="card-header bg-white border-bottom"><h2 id="exam-approval-heading" class="h5 mb-0">Completed exam results</h2></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Exam</th><th>Class / subject</th><th>Date</th><th>Marks</th><th>Conflicts</th><th>Approval</th><th>Action</th></tr></thead><tbody>@forelse($exams as $exam)<tr><td>{{ $exam->name }}</td><td>{{ $exam->schoolClass->name ?? 'N/A' }}<span class="d-block small text-muted">{{ $exam->subject->name ?? 'N/A' }}</span></td><td>{{ $exam->exam_date?->format('M j, Y') }}</td><td>{{ $exam->marks_count }}</td><td>@if($exam->schedule_conflicts)<span class="badge text-bg-warning">{{ $exam->schedule_conflicts }} same-slot</span>@else<span class="badge text-bg-success">None found</span>@endif</td><td>{{ Str::title($exam->resultsApproval?->status ?? 'pending') }}</td><td>@if(!$exam->resultsApproval || $exam->resultsApproval->status !== 'approved')<form method="POST" action="{{ route('registrar.exam-approvals.approve', $exam) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-success" type="submit" @disabled($exam->marks_count === 0)>Approve</button></form>@endif @if(!$exam->resultsApproval || $exam->resultsApproval->status !== 'rejected')<form method="POST" action="{{ route('registrar.exam-approvals.reject', $exam) }}" class="d-inline"><input type="hidden" name="notes" value="Returned for Registrar review"><button class="btn btn-sm btn-outline-danger" type="submit">Reject</button></form>@endif</td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">No completed exams require review.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $exams->links() }}</div></section></div>
@endsection
